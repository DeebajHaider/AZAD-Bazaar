import React from 'react'
import Cart from './Cart'

// Simple wrapper so /view-cart shows the same cart view (placeholder for recent orders view)
export default function ViewCart() {
  return <Cart />
}
